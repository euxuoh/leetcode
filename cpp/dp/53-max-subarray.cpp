#include <iostream>
#include <vector>

using namespace std;

int maxSubArray(vector<int> &nums) {
    int res = 0;

    return res;
}

int main() {
    vector<int> nums = {1, 2, 4, 7, 2, 1, 1, 0};

    int res = maxSubArray(nums);

    return 0;
}
